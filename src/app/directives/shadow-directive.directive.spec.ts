import { ShadowDirectiveDirective } from './shadow-directive.directive';

describe('ShadowDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ShadowDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
